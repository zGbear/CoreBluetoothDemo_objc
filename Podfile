# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'coreBluetoothRetrievePeripheral' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  # Pods for coreBluetoothRetrievePeripheral
  pod 'Bolts', '1.1.4'

  target 'coreBluetoothRetrievePeripheralTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'coreBluetoothRetrievePeripheralUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
